## 肉类食品溯源系统测试文档

> 说明：本测试文档基于当前项目实现，覆盖管理员端、车间端、仓库端的主要业务流程，可作为毕业设计论文或项目文档中的“系统测试”章节使用。

---

## 一、测试环境说明

- **前端**
  - 技术栈：Vue 3 + Vite + Element Plus
  - 启动命令（在 `frontend` 目录）：
    - 安装依赖：`npm install`
    - 启动开发服务器：`npm run dev`
  - 访问地址：`http://localhost:5173`

- **后端**
  - 技术栈：Spring Boot + MyBatis-Plus + MySQL
  - 启动命令（在 `backend` 目录）：
    - `mvn spring-boot:run`
  - 服务地址：`http://localhost:8080`

- **数据库**
  - 类型：MySQL 8
  - 数据库名：`mes`
  - 初始化脚本：`backend/src/main/resources/mes_full.sql`
  - 编码：`utf8mb4`

- **测试账号**
  - **管理员端**：用户名 `admin`，密码 `admin123`
  - **车间端**：用户名 `workshop1`，密码 `workshop123`
  - **仓库端**：用户名 `warehouse1`，密码 `warehouse123`

---

## 二、角色与权限说明

- **管理员（ADMIN）**
  - 登录后首页为管理端仪表盘
  - 主要权限：
    - 用户管理：启用/停用用户、重置密码
    - 基础数据：物料、客户、供应商、质量原因
    - 业务数据：订单、工单、库存查看
    - 采购管理：新建采购单、收货入库
    - 设备管理：设备列表与状态
    - 消息中心：预警消息查看与扫描
    - 报表：核心指标可视化与 CSV 导出

- **车间员（WORKSHOP）**
  - 登录后首页为“车间端 · 生产执行”
  - 主要权限：
    - 查看今日车间看板（计划 vs 实际完成）
    - 查看待生产工单
    - 生产报工录入
    - 查看“我的报工记录”
    - 历史工单查询（按状态筛选）
    - 作业指导书占位页面

- **仓管员（WAREHOUSE）**
  - 登录后首页为“仓库端 · 库存与出入库”
  - 主要权限：
    - 查看库存总览
    - 查看今日出入库记录
    - 执行入库/出库操作
    - 执行盘点调整

---

## 三、登录与入口测试

### 3.1 登录界面与基础交互

- **步骤**
  1. 打开前端地址 `http://localhost:5173`。
  2. 在登录页顶部切换“溯源节点 / 消费者 / 管理员”标签。
  3. 观察输入框占位提示是否与参考界面一致（企业编号、密码等）。

- **预期结果**
  - 标签切换样式明显，当前端高亮。
  - 输入框有清晰的占位文案和图标，错误提示为中文。

### 3.2 按角色登录成功流程

- **管理员端**
  - 步骤：选择“管理员”标签，输入 `admin/admin123` 登录。
  - 预期：跳转到管理端总览页面，顶部问候语为“您好，管理端 admin”。

- **车间端**
  - 步骤：选择“溯源节点”或车间入口，输入 `workshop1/workshop123` 登录。
  - 预期：跳转到车间端 · 生产执行页面。

- **仓库端**
  - 步骤：选择仓库入口，输入 `warehouse1/warehouse123` 登录。
  - 预期：跳转到仓库端 · 库存与出入库页面。

### 3.3 角色不匹配拦截

- **步骤**
  1. 在“管理员”入口下，用 `workshop1/workshop123` 尝试登录。
  2. 或在“车间端”入口下，用 `admin/admin123` 登录。

- **预期结果**
  - 系统提示“当前账号不是管理端账号”或相应的中文错误信息。
  - 不应进入错误角色的后台页面。

### 3.4 注册功能测试（车间端）

- **目标**
  - 验证前后端注册流程，确保新注册用户可作为车间端账号登录系统。

- **步骤**
  1. 打开登录页，点击“立即注册车间端账号”切换到注册模式。
  2. 输入新的用户名、密码，并重复确认密码。
  3. 点击“注册”按钮。
  4. 注册成功后，系统提示“注册成功，请使用新账号登录（车间端）”，并自动切回登录模式，带入刚才的用户名和密码。
  5. 直接点击“登录”，验证是否跳转到车间端首页。

- **预期结果**
  - 用户名重复时，系统提示“用户名已存在”，不创建重复账号。
  - 注册成功后，数据库 `sys_user` 表新增一条记录：
    - `username` 为注册输入值
    - `role_code` 固定为 `WORKSHOP`
    - `enabled` 为 1
  - 使用新账号在车间入口登录能够成功进入“车间端 · 生产执行”页面。

---

## 四、管理员端测试流程

### 4.1 总览/报表

- **目标**
  - 验证汇总数据是否与各子模块数据一致。
  - 验证图表展示与导出功能。

- **步骤**
  1. 登录管理员端，进入“总览/报表”Tab。
  2. 记录顶部三张卡片数值：
     - 订单数量
     - 工单数量
     - 物料种类
  3. 切换到“订单”“工单”“物料”Tab，统计实际列表条数。
  4. 观察“订单准时交付率 / 生产完成率 / 近30天出库总量”的数值与柱状图展示（其中订单号 `SO-VERIFY-xxx`、工单号 `WO-VERIFY-xxx`、业务单号 `VERIFY-IN/OUT-xxx` 为报表验证用示例数据，由 `mes_full.sql` 初始化脚本自动插入）。
  5. 点击“导出报表 CSV”，检查生成的 CSV 文件。

- **预期结果**
  - 三张卡片的数量与对应列表条数完全一致。
  - 指标说明：
    - 订单准时交付率 = 准时交付订单数 / 总订单数。
    - 生产完成率 = 已完成工单数 / 总工单数。
    - 近30天出库总量 = 近30天所有负向库存变动绝对值之和。
  - 柱状图：
    - 每个类目下的柱子居中对齐，对应类目标签在柱形正下方。
    - 柱形内部数值文字居中显示，比率保留 1 位小数，带 `%`。
  - “导出报表 CSV”生成的文件：
    - 包含订单、工单、库存三类数据。
    - 字段顺序和内容与系统中的列表一致。

### 4.2 用户管理

- **目标**
  - 验证用户启用/禁用和重置密码功能。

- **步骤**
  1. 切换到“用户管理”Tab。
  2. 检查角色列中文显示是否为“管理员 / 车间员 / 仓管员”。
  3. 对某个用户切换“启用”开关。
  4. 使用该用户账号重新登录，验证是否生效。
  5. 点击“重置密码”按钮，确认弹窗后，使用新密码尝试登录。

- **预期结果**
  - 角色列始终以中文显示。
  - 启用状态切换后，列表即时更新；被禁用用户无法正常登录。
  - 重置密码后，提示“已重置密码为 123456”，新密码可用。

### 4.3 物料 / 客户 / 供应商

- **物料**
  - 检查“类型”列为中文“原材料 / 成品”。
  - “启用”列显示为“是 / 否”，与数据库初始化数据一致。

- **客户**
  - 列表显示客户名称、联系人、电话、地址。
  - 字段内容与初始化 SQL 数据一致。

- **供应商**
  - 列表显示供应商名称、联系人、电话、地址。
  - “启用”列正确显示“是 / 否”。

### 4.4 订单与工单

- **订单列表**
  - 状态显示为中文：
    - PENDING → 待处理
    - PRODUCING → 生产中
    - DONE → 已完成
    - DELIVERED → 已交付
  - 订单号、客户ID、产品物料ID、数量等字段与数据库保持一致。

- **工单列表**
  - 状态显示为中文：
    - TO_PRODUCE → 待生产
    - PRODUCING → 生产中
    - DONE → 已完成
  - 工单与订单之间的关联（订单ID和工单号）正确。

### 4.5 库存与质量原因

- **库存**
  - “物料ID、数量、更新时间”与 `inventory_stock` 表内容一致。
  - 可与仓库端的入库/出库操作联动验证（见第六章）。

- **质量原因**
  - 列表包含如“划伤 / 脏污 / 尺寸不良”等初始化原因。
  - “启用”列显示“是 / 否”，“排序”列为整数，排序合理。

### 4.6 采购管理

- **新建采购单**
  - 步骤：
    1. 在“采购”Tab 中点击“新建采购单”。
    2. 观察弹窗内“采购单号”是否自动填入：`POYYYYMMDDHHMMSS`（可手动修改）。
    3. 选择供应商，选择预计到货日期，填写备注（可选）。
    4. 在“明细”区域选择物料，输入数量，点击“添加行”。
    5. 点击“确定”提交。
  - 预期：
    - 未填写采购单号时，提示“请输入采购单号”。
    - 未添加任一物料明细时，提示“请至少添加一行物料”。
    - 成功提交后弹出“创建成功”，列表中出现新采购单，状态为“已创建”。

- **全部收货**
  - 步骤：
    1. 在采购列表中找到一条非“已完成”的采购单。
    2. 点击“全部收货”，在弹出的确认框中点击“确定”。
  - 预期：
    - 弹出确认提示，确认后提示“收货完成”。
    - 采购单状态变为“已完成”。
    - 再次进入库存列表，对应物料库存数量增加。

### 4.7 设备与消息

- **设备**
  - 状态显示为中文：
    - IDLE → 空闲
    - RUNNING → 运行中
    - MAINTENANCE → 维护中
    - BROKEN → 故障

- **消息**
  - “扫描超期/库存预警”按钮：
    - 点击后调用后端扫描逻辑，触发库存低、工单超期等消息生成。
  - 消息列表：
    - 类型显示为“库存预警 / 工单超期”。
    - 级别显示为“提示 / 警告 / 错误”。
    - 状态显示为“已读 / 未读”。
    - “设为已读”按钮点击后，该条消息状态变为“已读”，按钮消失。

---

## 五、车间端测试流程

### 5.1 车间看板

- **目标**
  - 验证“今日计划数量”和“实际完成数量”的统计是否正确。

- **步骤**
  1. 使用 `workshop1` 登录车间端。
  2. 进入“车间看板”Tab。
  3. 查看柱状图中“计划数量 / 实际完成”。
  4. 对照数据库：
     - 当日 `due_date` 为今天的工单 `qty` 合计应等于“计划数量”。
     - 当日 `report_time` 为今天的报工记录 `good_qty` 合计应等于“实际完成”。

- **预期结果**
  - 图表中的两根柱形数值与对应数据库统计一致。

### 5.2 待生产工单

- **测试点**
  - 列表仅展示状态为 TO_PRODUCE（待生产）的工单。
  - 字段包括工单号、订单ID、产品物料ID、计划数量、计划完成时间。

### 5.3 生产报工

- **步骤**
  1. 在“生产报工”Tab 中，输入一个存在的工单号。
  2. 填写工序名称、合格数量、不良数量和不良原因。
  3. 点击“提交报工”。

- **预期结果**
  - 若工单号为空或合格数量 ≤ 0，给出相应的中文提示，不允许提交。
  - 报工成功后：
    - 弹出“报工成功”提示。
    - “我的报工记录”Tab 中出现对应记录。
    - 数据库 `production_report` 表新增一条记录。

### 5.4 我的报工记录与历史工单

- **我的报工记录**
  - 只展示当前登录用户的报工记录。
  - 工单号通过 ID 映射后显示为人类可读的工单号。

- **历史工单查询**
  - 状态筛选下拉框：
    - 待生产 / 生产中 / 已完成
  - 不同筛选条件下列表只显示对应状态的工单，状态列为中文。

### 5.5 作业指导书

- 当前为占位页面：
  - 文案为“作业指导书（按工单/产品查看）”。
  - 用于说明后续可接入文档或图片服务。

---

## 六、仓库端测试流程

### 6.1 库存总览

- **步骤**
  1. 使用 `warehouse1` 登录仓库端。
  2. 进入“库存总览”Tab。
  3. 对比数据库 `inventory_stock` 表，检查物料ID与数量是否一致。

- **预期结果**
  - 列表“物料”列显示物料编码/名称，数量与数据库完全一致。

### 6.2 今日出入库记录

- **步骤**
  1. 切换到“今日出入库记录”Tab。
  2. 检查记录的日期是否为今天。
  3. 检查“变化量”列颜色与符号：
     - 大于 0 为绿色，并带有 `+` 号。
     - 小于 0 为红色。

- **预期结果**
  - 所有记录均为当日出入库。
  - 显示的变化量与 `inventory_record` 表中当天记录一致。

### 6.3 入库/出库操作

- **入库测试**
  - 步骤：
    1. 进入“入库/出库”Tab。
    2. 在“入库”区域选择某物料，输入数量和业务单号。
    3. 点击“入库”按钮。
  - 预期：
    - 若未选择物料或数量 ≤ 0，提示“请选择物料并输入入库数量”。
    - 成功后提示“入库成功”，库存总览中的数量增加。
    - 今日出入库记录中新增一条入库记录，变化量为正数。

- **出库测试**
  - 步骤：
    1. 在“出库”区域选择某物料，输入数量和业务单号。
    2. 点击“出库”按钮。
  - 预期：
    - 若未选择物料或数量 ≤ 0，提示“请选择物料并输入出库数量”。
    - 成功后提示“出库成功”，库存总览中的数量减少。
    - 今日出入库记录中新增一条出库记录，变化量为负数。

### 6.3.1 工单待出库（一键出库）

- **步骤**
  1. 切换到"工单待出库"Tab。
  2. 查看待出库工单列表，列表显示：工单号、产品、需求数量、成品库存、原材料需求、出库方式。
  3. 系统自动判断出库方式：
     - **成品库存充足**：显示绿色"出成品"标签，点击一键出库将直接出库成品（SALES_OUT）
     - **成品不足但原材料充足**：显示黄色"出原材料"标签，点击一键出库将出库原材料供车间生产（PRODUCTION_OUT）
     - **成品和原材料都不足**：显示红色错误标签，按钮禁用
  4. 点击可出库工单的"一键出库"按钮。

- **预期结果**
  - **成品充足时**：
    - 弹出绿色确认框，显示"成品库存充足，直接出库成品"
    - 确认后出库成品，生成一条 `SALES_OUT` 类型的出库记录
  - **成品不足、原材料充足时**：
    - 弹出黄色确认框，显示"出库原材料供车间生产"及各原材料详情
    - 确认后批量出库原材料，每种原材料生成一条 `PRODUCTION_OUT` 类型的出库记录
    - 提示"原材料出库成功，请通知车间生产"
  - **全部不足时**：按钮禁用，无法点击

- **BOM关联说明**
  - 工单的产品ID（`productMaterialId`）对应 `base_bom` 表中的 `product_material_id`
  - BOM中的 `material_id` 为原材料ID，`qty` 为单位用量
  - 原材料出库数量 = BOM单位用量 × 工单需求数量

### 6.3.2 原材料入库（采购入库）

- **步骤**
  1. 进入"原材料入库"Tab。
  2. 在下拉框中选择一个原材料（只显示 `materialType = RAW` 的物料）。
  3. 选择后自动显示当前库存数量。
  4. 输入入库数量和可选的采购单号。
  5. 点击"入库"按钮。

- **预期结果**
  - 若未选择物料或数量 ≤ 0，提示"请选择物料并输入入库数量"。
  - 成功后提示"入库成功"，库存总览中的数量增加。
  - 今日出入库记录中新增一条 `PURCHASE_IN` 类型的入库记录，变化量为正数。

### 6.3.3 原材料出库（显示库存）

- **步骤**
  1. 进入"原材料出库"Tab。
  2. 选择关联工单。
  3. 在原材料下拉框中选择原材料（仅显示 `materialType = RAW` 的物料），下拉选项中会显示每个物料的当前库存。
  4. 选择后显示当前库存数量，如果出库数量超过库存，会显示红色"库存不足"提示。
  5. 输入出库数量，点击"出库"按钮。

- **预期结果**
  - 若未选择物料或数量 ≤ 0，提示"请选择物料并输入出库数量"。
  - 若出库数量 > 当前库存，提示"库存不足，出库失败"。
  - 成功后提示"出库成功"，库存总览中的数量减少。
  - 今日出入库记录中新增一条 `PRODUCTION_OUT` 类型的出库记录，变化量为负数。

### 6.3.4 成品出库（销售发货）

- **步骤**
  1. 进入"成品出库"Tab。
  2. 在成品下拉框中选择成品（仅显示 `materialType = PRODUCT` 的物料），下拉选项中会显示每个物料的当前库存。
  3. 选择后显示当前库存数量，如果出库数量超过库存，会显示红色"库存不足"提示。
  4. 输入出库数量和可选的销售单号。
  5. 点击"出库"按钮。

- **预期结果**
  - 若未选择物料或数量 ≤ 0，提示"请选择成品并输入出库数量"。
  - 若出库数量 > 当前库存，提示"库存不足，出库失败"。
  - 成功后提示"出库成功"，库存总览中的数量减少。
  - 今日出入库记录中新增一条 `SALES_OUT` 类型的出库记录，变化量为负数。

### 6.4 盘点调整

- **步骤**
  1. 进入“盘点调整”Tab。
  2. 选择某物料，系统自动带出“账面数量”。
  3. 输入一个不同的“实盘数量”，点击“提交盘点调整”。

- **预期结果**
  - 若实盘数量与账面数量相同，提示“实盘数量与账面一致，无需调整”。
  - 若不同：
    - 提示“盘点调整成功”。
    - 库存总览中该物料数量更新为实盘数量。
    - `inventory_record` 表中新增一条 ADJUST 类型的出入库记录。

---

## 七、通用测试项

### 7.1 权限与访问控制

- 使用不同角色账号访问不属于自身角色的功能或接口时：
  - 前端入口上已有角色限制。
  - 后端接口通过 Spring Security 的角色控制（如 `@PreAuthorize("hasRole('ADMIN')")`）防止越权访问。

### 7.2 空数据展示

- 在无任何数据时，各表格组件应显示中文“暂无数据”。
- 实现方式：
  - 通过在 `main.ts` 中启用 Element Plus 中文 locale，全局替换默认英文 `No Data`。

### 7.3 退出登录

- 顶部栏显示问候语：“您好，（当前端名） 用户名”。
- 点击“退出登录”按钮：
  - 清除本地认证信息（token / 用户名 / 角色）。
  - 根据当前角色跳转回对应登录入口（管理端/车间端/仓库端的登录页）。

---

## 八、测试用例记录建议

在实际测试执行时，建议以表格形式记录每个用例的执行情况，可使用如下字段：

- **用例编号**：如 `TC-ADMIN-OVERVIEW-001`
- **模块**：登录 / 总览 / 用户管理 / 采购 / 车间报工 / 仓库入库 等
- **前置条件**：所需初始数据、登录角色等
- **测试步骤**：按步骤编号描述
- **预期结果**：对应本测试文档中的预期行为
- **实际结果**：通过 / 不通过
- **备注**：缺陷编号或补充说明

根据本测试文档，可以很方便地整理出完整的 Excel/Markdown 用例表，用于项目答辩或测试归档。

---

## 九、三端数据联系演示

> 本章节通过一个完整的业务场景，演示管理端、车间端、仓库端三端之间的数据流转与关联关系。

### 9.1 业务场景概述

**场景**：客户下单 → 创建工单 → 原材料出库 → 车间生产报工 → 成品入库

**数据流向图**：

```
┌─────────────┐     创建      ┌─────────────┐     报工      ┌─────────────┐
│  客户订单   │ ──────────▶  │   生产工单   │ ──────────▶  │  报工记录   │
│ (管理端)    │              │ (管理端)     │              │ (车间端)    │
└──────┬──────┘              └──────┬──────┘              └──────┬──────┘
       │                            │                            │
       │                            ▼                            │
       │                     ┌─────────────┐                     │
       │     原材料出库      │   库存管理   │     成品入库        │
       │  ◀────────────────  │ (仓库端)    │  ◀──────────────────┘
       │                     └─────────────┘
       │                            │
       ▼                            ▼
┌─────────────┐              ┌─────────────┐
│  报表统计   │ ◀─────────── │ 出入库记录  │
│ (管理端)    │              │ (仓库端)    │
└─────────────┘              └─────────────┘
```

---

### 9.2 演示准备

1. **确保数据库已初始化**
   - 执行 `mes_full.sql` 初始化脚本
   - 系统已有示例物料（钢板、油漆、螺丝等）、客户、供应商数据

2. **准备三个浏览器标签页**
   - 标签页1：管理端（admin / admin123）
   - 标签页2：车间端（workshop1 / workshop123）
   - 标签页3：仓库端（warehouse1 / warehouse123）

3. **记录初始数据**
   - 管理端 → 总览/报表：记录当前订单数、工单数
   - 仓库端 → 库存总览：记录钢板（RM-STEEL）当前库存数量

---

### 9.3 演示步骤

#### 步骤1：管理端 - 查看现有订单和工单

| 操作 | 说明 |
|------|------|
| 1. 登录管理端 | 使用 admin / admin123 登录 |
| 2. 进入"订单管理"标签页 | 查看现有订单列表，记录订单数量 |
| 3. 进入"工单管理"标签页 | 查看现有工单列表，找到状态为"待生产"的工单 |
| 4. 记录工单号 | 例如：`WO-SAMPLE-001`，计划数量：100 |

**数据关联说明**：
- 每个工单关联一个订单（通过 `order_id` 字段）
- 工单指定要生产的产品物料（通过 `product_material_id` 字段）

---

#### 步骤2：仓库端 - 原材料出库

| 操作 | 说明 |
|------|------|
| 1. 登录仓库端 | 使用 warehouse1 / warehouse123 登录 |
| 2. 进入"库存总览"标签页 | 记录钢板（RM-STEEL）当前库存，例如：500 |
| 3. 切换到"入库/出库"标签页 | |
| 4. 在"出库"区域选择物料 | 选择"RM-STEEL / 钢板" |
| 5. 输入出库数量 | 例如：120（生产100件产品需要的原材料） |
| 6. 填写单号 | 填入工单号：`WO-SAMPLE-001` |
| 7. 点击"出库"按钮 | 提示"出库成功" |
| 8. 返回"库存总览" | 确认钢板库存减少：500 → 380 |
| 9. 查看"今日出入库记录" | 看到一条"生产领料"记录，变化量：-120 |

**数据关联说明**：
- 出库记录通过 `biz_id` 字段关联工单号
- 出库类型 `PRODUCTION_OUT` 表示生产领料
- 库存数量实时更新

---

#### 步骤3：车间端 - 查看待生产工单

| 操作 | 说明 |
|------|------|
| 1. 登录车间端 | 使用 workshop1 / workshop123 登录 |
| 2. 进入"待生产工单"标签页 | 找到工单 `WO-SAMPLE-001` |
| 3. 确认工单信息 | 计划数量：100，计划完成日期 |

**数据关联说明**：
- 车间端读取的工单数据与管理端一致（同一张表）
- 工单状态为 `TO_PRODUCE` 时出现在待生产列表

---

#### 步骤4：车间端 - 执行生产报工

| 操作 | 说明 |
|------|------|
| 1. 切换到"生产报工"标签页 | |
| 2. 输入工单号 | `WO-SAMPLE-001` |
| 3. 输入工序 | 例如：`冲压` |
| 4. 输入合格数量 | 例如：95 |
| 5. 输入不良数量 | 例如：5 |
| 6. 选择不良原因 | 例如：`划伤` |
| 7. 点击"提交报工" | 提示"报工成功" |
| 8. 切换到"我的报工记录" | 看到刚才提交的报工记录 |
| 9. 查看"车间看板" | 实际完成数量更新 |

**数据关联说明**：
- 报工记录通过 `work_order_id` 关联工单
- 报工记录通过 `reporter_user_id` 关联当前登录用户
- 不良原因关联质量原因字典表

---

#### 步骤5：仓库端 - 成品入库

| 操作 | 说明 |
|------|------|
| 1. 返回仓库端标签页 | |
| 2. 切换到"入库/出库"标签页 | |
| 3. 在"入库"区域选择物料 | 选择成品物料"FG-PROD-01 / 电机外壳" |
| 4. 输入入库数量 | 95（与报工合格数量一致） |
| 5. 填写单号 | 填入工单号：`WO-SAMPLE-001` |
| 6. 点击"入库"按钮 | 提示"入库成功" |
| 7. 查看"今日出入库记录" | 看到一条"采购入库"记录，变化量：+95 |

**数据关联说明**：
- 入库记录的 `biz_id` 可关联工单号，便于追溯
- 成品库存增加

---

#### 步骤6：管理端 - 查看数据汇总

| 操作 | 说明 |
|------|------|
| 1. 返回管理端标签页 | |
| 2. 进入"总览/报表"标签页 | |
| 3. 查看生产完成率图表 | 观察已完成/生产中/待生产的数量变化 |
| 4. 查看库存周转图表 | 观察近30天入库/出库数量 |
| 5. 进入"库存管理"标签页 | 确认库存数据与仓库端一致 |
| 6. 点击"导出报表"按钮 | 可导出订单、工单、库存的CSV文件 |

**数据关联说明**：
- 管理端的报表数据来自各业务表的汇总统计
- 报表实时反映三端的操作结果

---

### 9.4 数据关联验证清单

完成上述演示后，可通过以下清单验证三端数据的一致性：

| 验证项 | 管理端 | 车间端 | 仓库端 | 关联字段 |
|--------|--------|--------|--------|----------|
| 工单信息一致 | 工单管理列表 | 待生产工单列表 | - | `work_order_no` |
| 库存数据一致 | 库存管理列表 | - | 库存总览列表 | `material_id` |
| 报工记录可追溯 | - | 我的报工记录 | - | `work_order_id` |
| 出入库记录完整 | - | - | 今日出入库记录 | `biz_id`(工单号) |
| 报表数据准确 | 总览/报表 | 车间看板 | - | 汇总统计 |

---

### 9.5 演示要点总结

1. **订单→工单**：管理端创建订单后派生工单，工单通过 `order_id` 关联订单

2. **工单→报工**：车间端根据工单进行生产报工，报工记录通过 `work_order_id` 关联工单

3. **生产→库存**：
   - 仓库端出库（生产领料）：`biz_type = PRODUCTION_OUT`
   - 仓库端入库（成品入库）：`biz_type = PRODUCTION_IN`
   - 通过 `biz_id` 填入工单号实现关联追溯

4. **全局可见**：管理端报表汇总所有数据，实时反映各端操作结果

5. **数据一致性**：同一张数据表被多端读取，确保数据实时同步

---

### 9.6 扩展演示场景

如需进一步演示，可尝试以下扩展场景：

| 场景 | 涉及端 | 演示要点 |
|------|--------|----------|
| 采购入库流程 | 管理端 + 仓库端 | 管理端创建采购单 → 仓库端收货入库 |
| 库存预警触发 | 管理端 + 仓库端 | 出库使库存低于安全库存 → 管理端消息提醒 |
| 工单超期预警 | 管理端 + 车间端 | 工单超过计划完成日期 → 管理端消息提醒 |
| 盘点调整 | 仓库端 | 实盘数量与账面不符 → 自动生成调整记录 |
| 多次报工累计 | 车间端 | 同一工单多次报工 → 看板累计显示 |


